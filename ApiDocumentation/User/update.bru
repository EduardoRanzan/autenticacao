meta {
  name: update
  type: http
  seq: 4
}

put {
  url: {{host}}users/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "email": "email2",
    "senha": "senha2"
  }
}
