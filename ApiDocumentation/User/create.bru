meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{host}}users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "email": "email",
    "senha": "senha"
  }
}
