meta {
  name: autenticacao
  type: http
  seq: 1
}

post {
  url: {{host}}auth
  body: json
  auth: none
}

body:json {
  {
    "email": "email",
    "senha": "senha"
  }
}

script:post-response {
  key="token";
  value=res.body.token
  bru.setEnvVar(key,value)
}
